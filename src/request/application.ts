// import Joi from 'joi';
// import { baseList } from './base';

// export const Create = Joi.object({
//   title: Joi.string().min(2).max(20).required(),
//   name: Joi.string().min(2).max(20).required(),
//   type: Joi.number(),
//   remark: Joi.string().allow('').default('').optional(),
// });

// export const Update = Joi.object({
//   application_id: Joi.string().min(19).max(20).required(),
//   title: Joi.string().min(2).max(20),
//   status: Joi.boolean(),
//   remark: Joi.string(),
// });

// export const List = Joi.object(baseList);
